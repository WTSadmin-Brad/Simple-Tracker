/**
 * admin-layout.tsx
 * Layout component for admin-facing pages
 */

const AdminLayout = ({ children }) => {
  return (
    <div>
      {/* Placeholder for Admin Layout implementation */}
      {children}
    </div>
  );
};

export default AdminLayout;

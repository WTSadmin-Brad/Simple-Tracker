/**
 * employee-layout.tsx
 * Layout component for employee-facing pages
 */

const EmployeeLayout = ({ children }) => {
  return (
    <div>
      {/* Placeholder for Employee Layout implementation */}
      {children}
    </div>
  );
};

export default EmployeeLayout;

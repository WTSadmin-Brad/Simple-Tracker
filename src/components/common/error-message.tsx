/**
 * error-message.tsx
 * Common component for displaying error messages
 */

const ErrorMessage = ({ message }) => {
  return (
    <div className="text-red-500">
      {/* Placeholder for Error Message implementation */}
      {message}
    </div>
  );
};

export default ErrorMessage;
